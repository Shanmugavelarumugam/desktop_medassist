// signup_screen.dart is now unified inside login_screen.dart
// Both route paths `/login` and `/signup` are mapped to LoginScreen
// which dynamically swaps the forms using an AnimatedSwitcher for a high-end UX.
